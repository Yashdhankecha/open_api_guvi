"""
Honeypot Scam Detection & Intelligence Extraction — Package init.
Import the router to mount it in the main FastAPI app.

Usage in backend.py:
    from files.routes import router as honeypot_router
    app.include_router(honeypot_router)
"""
